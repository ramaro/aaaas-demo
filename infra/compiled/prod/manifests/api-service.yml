apiVersion: v1
kind: Service
metadata:
  labels:
    name: api
  name: api
  namespace: prod
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: api
  type: NodePort
